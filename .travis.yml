language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-amdapp' ]; then cd .. && eval "mv $currentfolder generator-amdapp" && cd generator-amdapp; fi

